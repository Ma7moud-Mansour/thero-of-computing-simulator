================================================================================
          شرح مشروع Theory of Computing Simulator بالتفصيل الكامل
================================================================================

📌 نظرة عامة (Overview)
========================
المشروع ده عبارة عن "محاكي لنظرية الحوسبة" (Theory of Computing Simulator).
بيسمح للمستخدم إنه يدخل Regular Expression (تعبير نمطي) أو Context-Free Grammar
ويشوف بصريًا إزاي الآلات المختلفة (NFA, DFA, PDA, TM) بتشتغل عليه خطوة بخطوة.

المشروع مبني بـ:
  - Backend: Python + FastAPI (السيرفر اللي بيعمل كل الحسابات)
  - Frontend: HTML + CSS + JavaScript (الواجهة اللي بتعرض الرسومات والمحاكاة)

📌 إزاي تشغل المشروع
======================
1. تثبت المكتبات:
   pip install fastapi uvicorn

2. تشغل السيرفر:
   python -m uvicorn api.main:app --reload
   (هيشتغل على http://127.0.0.1:8000)

3. تفتح ui/index.html في المتصفح أو تفتح http://127.0.0.1:8000 مباشرة


================================================================================
                           هيكل المشروع (Project Structure)
================================================================================

theory_of_computing/
│
├── app.py                  ← تطبيق Desktop قديم (PySide6) - مش مستخدم حاليًا
├── README.md               ← ملف التعليمات
├── .gitignore              ← ملفات Git المتجاهلة
│
├── core/                   ← الـ Classes الأساسية (Base Classes)
│   ├── state.py            ← تعريف الحالة State
│   └── automaton.py        ← تعريف الآلة الأساسية Automaton
│
├── automata/               ← تعريف أنواع الآلات المختلفة
│   ├── nfa.py              ← NFA (آلة غير حتمية)
│   ├── dfa.py              ← DFA (آلة حتمية)
│   ├── pda.py              ← PDA (آلة بمكدس)
│   ├── tm.py               ← TM (آلة تورنج)
│   ├── subset_construction.py  ← تحويل NFA لـ DFA بطريقة Subset Construction
│   └── dfa_to_tm.py        ← تحويل DFA لـ Turing Machine
│
├── regex/                  ← معالجة التعبيرات النمطية
│   ├── validation.py       ← التحقق من صحة الـ Regex
│   ├── regex_parser.py     ← إدراج عملية التسلسل (Concatenation)
│   ├── postfix.py          ← تحويل Regex من Infix لـ Postfix
│   └── thompson.py         ← بناء NFA من Regex (خوارزمية Thompson)
│
├── cfg/                    ← معالجة القواعد الخالية من السياق
│   ├── grammar.py          ← تعريف الـ Grammar
│   ├── parser.py           ← محلل نحوي (Parser) مع Parse Tree
│   └── parse_tree.py       ← تعريف عقدة شجرة التحليل
│
├── conversions/            ← التحويلات بين الآلات
│   ├── nfa_to_dfa.py       ← تحويل NFA → DFA
│   ├── nfa_to_pda.py       ← تحويل NFA → PDA
│   ├── dfa_to_pda.py       ← تحويل DFA → PDA
│   ├── dfa_to_tm.py        ← تحويل DFA → TM
│   └── cfg_to_pda.py       ← تحويل CFG → PDA
│
├── simulation/             ← المحاكيات (Simulators)
│   ├── nfa_simulator.py    ← محاكاة NFA خطوة بخطوة
│   ├── dfa_simulator.py    ← محاكاة DFA خطوة بخطوة
│   ├── pda_simulator.py    ← محاكاة PDA خطوة بخطوة
│   └── tm_simulator.py     ← محاكاة Turing Machine خطوة بخطوة
│
├── api/                    ← السيرفر (FastAPI Backend)
│   └── main.py             ← كل الـ API Endpoints
│
└── ui/                     ← الواجهة الأمامية (Frontend)
    ├── index.html           ← الصفحة الرئيسية
    ├── style.css            ← الأنماط (مش مستخدم كتير - الأنماط في HTML)
    └── app.js               ← كود JavaScript الرئيسي (1360 سطر)


================================================================================
                     شرح كل ملف ووظيفته بالتفصيل
================================================================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 core/ - الأساسيات
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 core/state.py - تعريف الحالة (State)
-----------------------------------------
- فيه class اسمه State
- كل حالة ليها اسم (name) زي q0, q1, q2...
- فيه Auto-ID: لو مأعطيتوش اسم، هيتسمى تلقائيًا q0, q1, q2...
- الـ _id ده class variable مشترك بين كل الـ States عشان الترقيم التلقائي

📄 core/automaton.py - الآلة الأساسية (Base Automaton)
-------------------------------------------------------
- class أساسي اسمه Automaton
- كل آلة عندها:
  • states: مجموعة الحالات
  • alphabet: الأبجدية (الحروف المسموح بيها)
  • start_state: حالة البداية
  • accept_states: حالات القبول


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 automata/ - تعريف أنواع الآلات
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 automata/nfa.py - NFA (Nondeterministic Finite Automaton)
------------------------------------------------------------
- بيرث من Automaton
- transitions: قاموس (dictionary) المفتاح فيه = (state, symbol) والقيمة = set من الحالات
- symbol ممكن يكون None (يعني epsilon/ε transition)
- add_transition(): بتضيف انتقال جديد

📄 automata/dfa.py - DFA (Deterministic Finite Automaton)
----------------------------------------------------------
- بيرث من Automaton
- transitions: قاموس المفتاح = (state, symbol) والقيمة = حالة واحدة بس (مش set)
- الفرق عن NFA: كل حالة + رمز → حالة واحدة فقط

📄 automata/pda.py - PDA (Pushdown Automaton)
----------------------------------------------
- آلة بمكدس (Stack)
- عندها:
  • input_alphabet: أبجدية المدخلات
  • stack_alphabet: أبجدية المكدس
  • start_stack_symbol: الرمز اللي بيبدأ بيه المكدس (افتراضيًا "$" أو "Z0")
- transitions: المفتاح = (state, input_symbol, stack_top) والقيمة = (next_state, push_symbols)
- add_transition(state, inp, stack_top, next_state, push): بتضيف انتقال

📄 automata/tm.py - TM (Turing Machine)
-----------------------------------------
- آلة تورنج
- عندها transitions, start_state, accept_states
- الانتقالات بتاعتها = (state, read_symbol) → (next_state, write_symbol, direction)

📄 automata/subset_construction.py - تحويل NFA → DFA
------------------------------------------------------
- بتطبق خوارزمية Subset Construction
- الخطوات:
  1. حالة البداية في DFA = epsilon_closure(q0) في NFA
  2. لكل مجموعة حالات + رمز → move ثم epsilon_closure
  3. لو المجموعة الجديدة مش موجودة → أضفها كحالة DFA جديدة
  4. حالة DFA تكون accepting لو فيها أي حالة NFA accepting
- الحالات في DFA بتتسمى D0, D1, D2...
- بترجع dict فيه: states, transitions, start, accept, state_map

📄 automata/dfa_to_tm.py - تحويل DFA → Turing Machine
--------------------------------------------------------
- بتاخد DFA (كـ dict) وتحوله لـ TM
- كل transition في DFA → transition في TM (read → write نفس الرمز + تحرك يمين R)
- لما يوصل لنهاية الـ input (يقرأ blank "_"):
  • لو حالة accepting → يروح q_accept
  • لو مش accepting → يروح q_reject


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 regex/ - معالجة التعبيرات النمطية
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 regex/validation.py - التحقق من صحة الـ Regex
-------------------------------------------------
بتتحقق من:
  1. الـ Regex مش فاضي
  2. كل الحروف صالحة (حروف + أرقام + |*())
  3. الأقواس متوازنة ومفيش أقواس فاضية ()
  4. العمليات في أماكنها الصحيحة:
     - | مش في الأول أو الآخر
     - * مش في الأول
     - مفيش || أو |* أو (| أو |) متتابعين
     - * مش بعد ( أو |

📄 regex/regex_parser.py - إدراج عملية التسلسل (Concatenation)
---------------------------------------------------------------
- الـ Regex ملوش رمز صريح للتسلسل (concatenation)
- الدالة insert_concatenation() بتضيف "." بين الحروف اللي محتاجة تسلسل
- مثال: "ab" → "a.b" و "(a|b)*ab" → "(a|b)*.a.b"
- القاعدة: لو الحرف الحالي مش (|() والحرف اللي بعده مش (|)*) → أضف "."

📄 regex/postfix.py - تحويل Infix → Postfix
---------------------------------------------
- خوارزمية Shunting-Yard
- الأولويات: * = 3 (أعلى)، . = 2، | = 1 (أقل)
- مثال: "a.b|c" → "ab.c|"

📄 regex/thompson.py - بناء NFA من Regex (Thompson's Construction)
-------------------------------------------------------------------
- الخوارزمية الأساسية في المشروع!
- بتاخد الـ Regex بعد تحويله لـ Postfix وتبني NFA
- بتستخدم Stack وكل عملية بتاخد Fragment/Fragments وتنتج Fragment جديد

العمليات:
  • حرف عادي (a, b, ...): أنشئ حالتين s_start و s_end مع transition بالحرف
  • تسلسل ".": وصل accept states بتاعت f1 بـ start بتاعت f2 بـ ε
  • اتحاد "|": أنشئ s0 جديدة → ε → بداية f1 وf2، ونهايتهم → ε → f0 جديدة
  • نجمة "*" (Kleene Star): أنشئ s0 وf0:
    - s0 → ε → بداية f1 (ادخل)
    - s0 → ε → f0 (اقبل الفراغ)
    - نهاية f1 → ε → f0 (اخرج)
    - نهاية f1 → ε → بداية f1 (كرر)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 cfg/ - القواعد الخالية من السياق
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 cfg/grammar.py - تعريف الـ Grammar
---------------------------------------
- class Grammar عنده:
  • start: رمز البداية (مثلاً "S")
  • productions: قاموس {non-terminal → list of productions}
  • add_production(lhs, rhs): أضف قاعدة إنتاج

📄 cfg/parse_tree.py - عقدة شجرة التحليل
------------------------------------------
- class ParseTreeNode عنده:
  • symbol: الرمز (مثلاً "S" أو "a")
  • children: أولاده في الشجرة
  • add_child(): أضف ابن

📄 cfg/parser.py - المحلل النحوي (Parser)
-------------------------------------------
دالتين:
1. parse_string(grammar, string): Recursive Descent Parser بسيط
   - بيرجع True/False بس

2. parse_with_tree(grammar, string): نفس الـ Parser بس بيبني شجرة
   - بيرجع (accepted: bool, tree: ParseTreeNode)
   - بيجرب كل production لكل non-terminal
   - لو الـ production فاضي → يضيف عقدة ε


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 conversions/ - التحويلات بين الآلات
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 conversions/nfa_to_dfa.py - تحويل NFA → DFA (بالكائنات)
------------------------------------------------------------
- نسخة تانية من subset construction بس بترجع DFA object مش dict
- بتستخدم epsilon_closure و move من nfa_simulator

📄 conversions/nfa_to_pda.py - تحويل NFA → PDA
-------------------------------------------------
- ببساطة: بينقل كل transitions بتاعت NFA لـ PDA
- المكدس مبيتغيرش (بيقرأ Z0 ويكتب Z0 تاني)
- يعني الـ PDA بيتصرف زي NFA بالظبط بس مع Stack مبيستخدمهوش

📄 conversions/dfa_to_pda.py - تحويل DFA → PDA
-------------------------------------------------
- نفس الفكرة: DFA + مكدس مش بيتغير

📄 conversions/dfa_to_tm.py - تحويل DFA → TM (بالكائنات)
----------------------------------------------------------
- كل transition → نفسها + write نفس الرمز + move R
- على blank → يوقف (Stay)

📄 conversions/cfg_to_pda.py - تحويل CFG → PDA
-------------------------------------------------
- بيعمل PDA بحالة واحدة "q"
- لكل production A → α:
  • أضف transition: (q, ε, A) → (q, α) [يعني pop A و push α]
- لكل terminal a:
  • أضف transition: (q, a, a) → (q, ε) [يعني لو قرأ a وفي المكدس a → pop]
- القبول بـ empty stack


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 simulation/ - المحاكيات
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 simulation/nfa_simulator.py - محاكاة NFA
---------------------------------------------
ثلاث دوال أساسية:

1. epsilon_closure(nfa, states):
   - بتاخد مجموعة حالات وترجع كل الحالات اللي ممكن توصلها بـ ε فقط
   - بتستخدم Stack/DFS

2. move(nfa, states, symbol):
   - بتاخد مجموعة حالات + رمز وترجع الحالات اللي بتوصلها بالرمز ده

3. simulate_nfa(nfa, input_string):
   - المحاكاة الفعلية خطوة بخطوة:
     a. ابدأ بـ epsilon_closure({q0})
     b. لكل حرف: move ثم epsilon_closure
     c. في الآخر: لو أي حالة حالية في accept_states → accepted
   - بترجع history فيها كل خطوة (الحالات النشطة + الانتقالات)

📄 simulation/dfa_simulator.py - محاكاة DFA
----------------------------------------------
- أبسط من NFA لأن كل خطوة فيها حالة واحدة بس
- بيدور على الـ transition المناسب في الـ list
- لو مفيش transition → dead state (رفض)

📄 simulation/pda_simulator.py - محاكاة PDA
----------------------------------------------
دالتين:

1. simulate_pda(): محاكاة بسيطة لـ PDA اللي جاي من NFA
   - بتشتغل زي NFA simulator بس مع Stack ثابت (Z0)

2. simulate_general_pda(): محاكاة كاملة لأي PDA
   - بتستخدم Priority Queue (BFS ذكي)
   - الأولوية = طول المدخل المتبقي (عشان يلاقي الحل الأقصر)
   - حد أقصى 1000 خطوة عشان ميدخلش في loop لا نهائي
   - القبول: لو accept_by_empty_stack=True → المكدس لازم يفضى
   - دي بتتستخدم مع CFG → PDA

📄 simulation/tm_simulator.py - محاكاة Turing Machine
-------------------------------------------------------
- بيمثل الـ tape كـ list من الحروف
- head: مكان الرأس على الشريط
- خطوات المحاكاة:
  1. اقرأ الرمز تحت الرأس
  2. دور على transition مناسب
  3. اكتب الرمز الجديد
  4. حرك الرأس (R يمين / L شمال / S ثابت)
  5. لو وصلت q_accept → accepted، q_reject → rejected
- حد أقصى 5000 خطوة
- بيرجع history فيها كل خطوة (حالة الشريط + مكان الرأس + الانتقال)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 api/ - السيرفر (FastAPI Backend)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 api/main.py - كل الـ API Endpoints
---------------------------------------

الـ Models (أنواع البيانات المتوقعة):
  • regexInput: {regex: str}
  • SimulateInput: {regex: str, string: str}
  • SimulateTMInput: {tm: dict, string: str}
  • CFGInput: {grammar: dict, start: str, string: str}

الـ Helper Functions:
  • serialize_nfa(): تحويل NFA object → JSON
  • normalize_nfa(): إعادة ترقيم الحالات بـ BFS عشان تكون منظمة (q0, q1, q2...)
  • serialize_pda(): تحويل PDA object → JSON
  • serialize_tree(): تحويل Parse Tree → JSON
  • get_leftmost_derivation(): استخراج الاشتقاق اليساري من الشجرة

الـ Endpoints:

1. POST /nfa          → بناء NFA من Regex
   الخطوات: validate → insert_concatenation → to_postfix → regex_to_nfa → normalize → serialize

2. POST /simulate/nfa → محاكاة NFA على input string
   الخطوات: بناء NFA + simulate_nfa → {accepted, steps}

3. POST /dfa          → بناء DFA من Regex
   الخطوات: بناء NFA → subset_construction → DFA dict

4. POST /simulate/dfa → محاكاة DFA
   الخطوات: بناء DFA + simulate_dfa → {accepted, steps}

5. POST /build_tm     → بناء Turing Machine من Regex
   الخطوات: بناء NFA → DFA → dfa_to_tm → TM dict

6. POST /simulate/tm  → محاكاة Turing Machine
   الخطوات: simulate_tm(tm_dict, string) → {accepted, steps}

7. POST /pda          → بناء PDA من Regex
   الخطوات: بناء NFA → nfa_to_pda → serialize_pda

8. POST /simulate/pda → محاكاة PDA
   الخطوات: بناء NFA → NFA→PDA → simulate_pda → {accepted, steps}

9. POST /cfg/parse    → تحليل string بـ CFG
   الخطوات: بناء Grammar → parse_with_tree → {accepted, tree, derivations}

10. POST /cfg/pda     → تحويل CFG لـ PDA
    الخطوات: بناء Grammar → cfg_to_pda → serialize_pda

11. POST /simulate/cfg/pda → محاكاة PDA المبني من CFG
    الخطوات: بناء Grammar → cfg_to_pda → simulate_general_pda → {accepted, steps}

Static Files:
  • GET / → يرجع ui/index.html
  • /static/* → يقدم ملفات ui/


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 ui/ - الواجهة الأمامية (Frontend)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 ui/index.html - الصفحة الرئيسية
-------------------------------------
التصميم: Dark Theme شبه VS Code

المكونات:
1. Sidebar (شريط جانبي) فيه:
   - عنوان "Theory Studio"
   - Tabs: "Regex Lab" و "CFG Lab"

   Tab 1 - Regex Lab:
   - حقل إدخال الـ Regex (افتراضي: (a|b)*ab)
   - 4 أزرار: NFA, DFA, PDA, TM (لبناء الآلة)
   - حقل إدخال Test Input (افتراضي: aab)
   - زر Simulate (لتشغيل المحاكاة)
   - Input Tracker (بيوريك الحروف وإيه اتقرأ)
   - Status Panel (بيوري الخطوة الحالية)
   - Playback Controls (Prev ◀ / Next ▶)

   Tab 2 - CFG Lab:
   - حقل إدخال الـ Grammar (textarea)
   - حقل إدخال Test String
   - 3 أزرار: Parse String, Convert to PDA, Simulate PDA
   - نتيجة التحليل + Leftmost Derivation
   - شجرة التحليل بتترسم في الـ Canvas

2. Main View (المنطقة الرئيسية):
   - SVG Canvas كبير لرسم الـ Graph
   - خلفية نقطية (dot grid)
   - Tape Visualization (لآلة تورنج) في الأسفل
   - Stack Visualization (للـ PDA) على اليمين

📄 ui/app.js - كود JavaScript الرئيسي (1360 سطر)
----------------------------------------------------
مقسم لـ 3 أجزاء رئيسية:

1. LayoutEngine (محرك التخطيط):
   - بيحسب إحداثيات الحالات على الشاشة
   - بيستخدم Layered Graph Layout (BFS)
   - بيقسم الحالات لطبقات (layers) حسب بعدها عن البداية
   - الإعدادات: xSpacing=250, ySpacing=180, startX=150, centerY=400

2. Renderer (محرك الرسم):
   - بيرسم الـ Graph كله على SVG
   - drawState(): بيرسم كل حالة (دائرة + اسم)
     • الحالة العادية: حدود زرقاء
     • حالة القبول: حدود خضراء + دائرة داخلية
     • q_accept: أخضر
     • q_reject: أحمر
     • حالة البداية: ليها سهم داخل
   - drawEdges(): بيرسم الانتقالات
     • Self-loops: منحنى فوق الحالة
     • Bidirectional: منحنيات متقابلة
     • Simple: خط مستقيم (لو مفيش تقاطع)
     • Curved: لو في تقاطع أو edges متعددة
   - Drag & Drop: تقدر تسحب أي حالة وتحركها!
     • handleMouseDown/Move/Up
     • updateConnectedEdges(): بيحدّث الخطوط لما تحرك حالة
   - highlight(): بيلون الحالات النشطة والانتقالات في المحاكاة
     • الحالات النشطة: خلفية زرقاء
     • الانتقالات النشطة: أخضر + سُمك أكبر

3. Simulator (المحاكي):
   - loadNFA/DFA/PDA/TM(): بيبعت request للـ API ويرسم النتيجة
   - runSimulation(): بيبعت request للمحاكاة ويخزن الـ history
   - next/prev(): بيتحرك بين خطوات المحاكاة
   - updateView(): بيحدّث العرض في كل خطوة:
     • Status text + Step counter
     • Highlight الحالات والانتقالات
     • Input Tracker: الحروف المقروءة (أخضر) + الحالي (أصفر) + المتبقي (رمادي)
     • Tape (لـ TM): شريط فيه الخلايا والـ head
     • Stack (لـ PDA): عمود بعناصر المكدس
   - renderTape(): بيعرض 13 خلية (6 قبل الرأس + الحالية + 6 بعدها)
   - renderStack(): بيعرض عناصر المكدس عمودياً
   - Acceptance/Rejection: في آخر خطوة بيحدد لو accepted ✅ أو rejected ❌

   CFG Functions:
   - parseGrammarInput(): بيحلل نص الـ Grammar من الـ textarea
   - parseCFG(): بيبعت للـ API ويعرض النتيجة + الشجرة
   - drawTree(): بيرسم Parse Tree على الـ Canvas
   - buildCFGPDA(): بيبني PDA من CFG ويرسمه
   - simulateCFG(): بيشغل محاكاة PDA على الـ string

   Keyboard Shortcuts:
   - ArrowRight → الخطوة التالية
   - ArrowLeft → الخطوة السابقة

   عند التحميل (window.onload):
   - بيبني NFA تلقائيًا من الـ Regex الافتراضي "(a|b)*ab"


================================================================================
                     تدفق البيانات (Data Flow)
================================================================================

المسار الكامل من إدخال Regex لحد المحاكاة:

1. المستخدم يكتب Regex: "(a|b)*ab"
                    ↓
2. validate_regex() → يتأكد الـ Regex صحيح
                    ↓
3. insert_concatenation() → "(a|b)*.a.b"
                    ↓
4. to_postfix() → "ab|*a.b."
                    ↓
5. regex_to_nfa() → NFA Object (Thompson's Construction)
                    ↓
6. normalize_nfa() → إعادة ترقيم الحالات (q0, q1, q2...)
                    ↓
7. serialize_nfa() → JSON Response للـ Frontend
                    ↓
8. LayoutEngine.compute() → إحداثيات كل حالة
                    ↓
9. Renderer.draw() → رسم الـ Graph على SVG

لو المستخدم ضغط "DFA":
  الخطوات 1-6 + nfa_to_dfa(subset_construction) → DFA → رسم

لو المستخدم ضغط "TM":
  الخطوات 1-6 + NFA→DFA → DFA→TM → رسم

لو المستخدم ضغط "Simulate":
  بناء الآلة + simulate_xxx() → history → عرض خطوة بخطوة


================================================================================
                     الخوارزميات المستخدمة
================================================================================

1. Thompson's Construction: بناء NFA من Regex
2. Subset Construction: تحويل NFA → DFA
3. Recursive Descent Parsing: تحليل CFG
4. BFS: تخطيط الرسم البياني + إعادة ترقيم الحالات
5. Priority Queue (Dijkstra-like): محاكاة PDA العامة
6. Shunting-Yard Algorithm: تحويل Infix → Postfix


================================================================================
                     ملاحظات مهمة
================================================================================

1. app.py: ده تطبيق Desktop قديم بـ PySide6 (Qt) - مش مستخدم في النسخة الحالية
2. فيه نسختين من بعض التحويلات:
   - automata/subset_construction.py (بترجع dict - مستخدمة في API)
   - conversions/nfa_to_dfa.py (بترجع DFA object - مش مستخدمة مباشرة في API)
   - automata/dfa_to_tm.py (بترجع dict) vs conversions/dfa_to_tm.py (بترجع TM object)
3. الـ Frontend بيتواصل مع الـ Backend عن طريق fetch API (REST)
4. الـ CORS مفتوح لكل المواقع (allow_origins=["*"])
5. الـ States بتتعمل drag & drop في الواجهة
6. Keyboard shortcuts: Arrow keys للتنقل بين الخطوات

================================================================================
